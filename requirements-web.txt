# Minimal deps for web_api/main.py's deployed service. /gate/demo
# imports agent.gate.verify + corpus.policy_records (pure Python, no
# numpy/torch needed); /gate/live-demo additionally needs the real moss
# SDK. livekit (api only, not
# livekit-agents/plugins) is needed for /token's issue_room_token --
# lightweight, no torch pulled in. IMPORTANT: the top-level `livekit`
# package does NOT include the `api` submodule -- that's the separate
# `livekit-api` package, confirmed by reproducing the ImportError in a
# clean venv matching this exact file. Keeping this separate from
# requirements.txt (the full dev/test environment) so the deployed
# free-tier Render service builds fast and stays within its memory limit.
fastapi
livekit
livekit-api
moss
python-dotenv
uvicorn[standard]
